# LLM Bench — install into the SAME venv that runs the server:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#   pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
fastapi>=0.110
uvicorn[standard]>=0.29
psutil>=5.9
requests>=2.31
openpyxl>=3.1
python-multipart>=0.0.9   # needed for file uploads
pypdf>=4.0                # PDF text extraction
python-docx>=1.1          # Word (.docx) text extraction
pillow>=10.0              # image metadata (OCR additionally needs: apt install tesseract-ocr && pip install pytesseract)
nvidia-ml-py>=12.535      # optional: used when an NVIDIA GPU is present (Jetson uses tegrastats/sysfs instead)
# llama-cpp-python is installed separately (see above) because it ships on a custom wheel index.
# For NVIDIA CUDA inference instead of the CPU build:
#   CMAKE_ARGS="-DGGML_CUDA=on" pip install llama-cpp-python
# or prebuilt: --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu124
